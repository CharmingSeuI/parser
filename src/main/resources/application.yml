spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/copus
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
    hikari:
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create